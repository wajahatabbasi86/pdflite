# Add project-specific ProGuard rules here.
# PdfBox-Android and AndroidX libraries ship their own consumer rules; nothing
# custom is required for v1. Rules will be added if release minification is
# enabled and something breaks at runtime.
